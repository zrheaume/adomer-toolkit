
class Profiler {
   constructor(validator) {
      // Create validator profile
      this.validator = validator
   }

   // Using the ATKmap validator profile, we can begin to create a Profile
   // on our app
   makeFlat(nest) {
      let _flat = []
      let mapdata = this.validator.mapdata


      


   }

}

module.exports = {
   Profiler
}